# backup default
cp /usr/local/etc/rc.d/redis /usr/local/etc/rc.d/redis.backup

# reconfigure daemon file
sed -i -r 's/redis_enable="NO"/redis_enable="YES"/' /usr/local/etc/rc.d/redis

# restart configd to unload plugin actions
service configd restart